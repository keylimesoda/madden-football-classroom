# GitHub Pages deployment surface

GitHub Pages serves this directory because branch-based Pages supports only the repository root or `/docs`.

The editable prototype source is also kept at `site/index.html` while the project uses this no-build deployment mode. Keep the two files identical until the project moves to a build workflow.
